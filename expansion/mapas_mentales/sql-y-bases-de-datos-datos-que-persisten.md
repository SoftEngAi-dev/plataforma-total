# 🧠 Mapa mental — SQL y Bases de Datos — Datos que Persisten

```
SQL y Bases de Datos — Datos que Persisten
├─ Bases de datos y SQL: el lenguaje de los datos
│   └─ 💬 ¿Qué hace SELECT nombre, precio FROM productos?
├─ SELECT con WHERE: filtrar con precisión
│   └─ 💬 ¿Cómo verificar si un campo es NULL?
├─ Ordenar, limitar y funciones agregadas
│   └─ 💬 ¿Qué devuelve COUNT(*)?
├─ GROUP BY y HAVING: resúmenes por grupos
│   └─ 💬 ¿Diferencia entre WHERE y HAVING?
├─ CREATE, INSERT, UPDATE, DELETE: escribir datos
│   └─ 💬 ¿Por qué UPDATE sin WHERE es un desastre clásico?
├─ Relaciones: FOREIGN KEY y primer diseño
│   └─ 💬 ¿Dónde va la FK en una relación 1 a muchos (autor-libros)?
├─ JOINs: consultar varias tablas
│   └─ 💬 ¿Qué registros incluye LEFT JOIN que INNER no?
├─ Índices y transacciones: velocidad y seguridad
│   └─ 💬 ¿Cuándo crear un índice?
├─ SQL con Python: sqlite3 práctico
│   └─ 💬 ¿Por qué usar ? (parámetros) y nunca f-strings en SQL?
└─ Diseño final: modelar una app real en SQL
│   └─ 💬 ¿Por qué CASCADE en comentarios de un post borrado?
```

🖨 Versión imprimible para tu pared: jerarquía = recorrido visual de memoria.