# ⚠️ Errores comunes — 1. Java: el gigante tipado (fundamentos en 15 min)

> Java y Spring — El Backend del Mundo Empresarial · Lección 1 · Aprender de los errores (propios y ajenos)

- ❌ «cualquier función» → Frente a «¿Dónde empieza a ejecutar un programa Java?» lo fácil es confundirse. **Verdad**: El método public static void main(String[] args). La firma exacta main es el punto de entrada universal de Java.
- ❌ «En la clase» → Frente a «¿Dónde empieza a ejecutar un programa Java?» lo fácil es confundirse. **Verdad**: El método public static void main(String[] args). La firma exacta main es el punto de entrada universal de Java.
- ❌ «Editor» → Frente a «¿Qué es la JVM?» lo fácil es confundirse. **Verdad**: La máquina virtual que ejecuta el bytecode Java haciéndolo portable (una compilation, corre en to-do SO con JVM). Compile once run anywhere: el bytecode .class corre en cualquier JVM.
- ❌ «Una librería» → Frente a «¿Qué es la JVM?» lo fácil es confundirse. **Verdad**: La máquina virtual que ejecuta el bytecode Java haciéndolo portable (una compilation, corre en to-do SO con JVM). Compile once run anywhere: el bytecode .class corre en cualquier JVM.

## 🩹 Antídoto universal
1. Lee el mensaje de error COMPLETO antes de googlear.
2. Pon el error exacto a la IA local y pide explicación, no solución.
3. Documenta el error en tu bitácora: error → causa → fix.
