# ⚠️ Errores comunes — 1. Bases de datos y SQL: el lenguaje de los datos

> SQL y Bases de Datos — Datos que Persisten · Lección 1 · Aprender de los errores (propios y ajenos)

- ❌ «Crea la tabla» → Frente a «¿Qué hace SELECT nombre, precio FROM productos?» lo fácil es confundirse. **Verdad**: Devuelve solo esas dos columnas de la tabla productos. SELECT = leer columnas elegidas de una tabla: la consulta más básica.
- ❌ «Inserta datos» → Frente a «¿Qué hace SELECT nombre, precio FROM productos?» lo fácil es confundirse. **Verdad**: Devuelve solo esas dos columnas de la tabla productos. SELECT = leer columnas elegidas de una tabla: la consulta más básica.
- ❌ «Es para datos simples nada más» → Frente a «¿Por qué SQLite es ideal para aprender/apps locales?» lo fácil es confundirse. **Verdad**: Zero-config: la base ES un archivo; sin servidor, incluida en Python y en todos los SO. Sin instalar servidor, pero con SQL completo: perfecta para apps desktop/móvil y aprender.
- ❌ «No soporta SQL real» → Frente a «¿Por qué SQLite es ideal para aprender/apps locales?» lo fácil es confundirse. **Verdad**: Zero-config: la base ES un archivo; sin servidor, incluida en Python y en todos los SO. Sin instalar servidor, pero con SQL completo: perfecta para apps desktop/móvil y aprender.

## 🩹 Antídoto universal
1. Lee el mensaje de error COMPLETO antes de googlear.
2. Pon el error exacto a la IA local y pide explicación, no solución.
3. Documenta el error en tu bitácora: error → causa → fix.
