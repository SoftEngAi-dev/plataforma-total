# 📖 Glosario — 4. Content Collections: Markdown con tipos

> 🚀 Astro — La Web de Contenido Moderna · Lección 4 · Términos que debes poder definir sin mirar

- **El frontmatter de cada Markdown durante el build** — Si un post incumple el esquema, el build falla: contenido corrupto nunca llega a producción.
- **getCollection('blog')** — getCollection devuelve las entradas tipadas; con entry.render() obtienes el HTML.

✍️ Ejercicio: añade debajo TU propia definición de cada término.
