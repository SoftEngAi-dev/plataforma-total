# 📕 Resumen maestro — Svelte y SvelteKit — Menos Código, Mismo Poder

> Todo el curso en una hoja: una idea núcleo por lección. Releer semanal.

## 1. 1. Svelte: el compilador que no envía framework
SVELTE: TU CÓDIGO SE CONVIERTE EN JS PURO VANILLA ━━━━━━━━━━━━━━━━━━━━━━━━━━━ Idea radical: React/Vue envían su runtime al navegador; Svelte COMPILA tus .svelte a JavaScript puro q…

## 2. 2. Reactividad total: stores, bindings y formularios
EL MODELO REACTIVO COMPLETO DE SVELTE ━━━━━━━━━━━━━━━━━━━━━━━━━━━ BINDINGS DOBLE VÍA (sin el rito de React)   <input bind:value={nombre}>          <!-- escribes → variable cambia; …

## 3. 3. SvelteKit y proyecto: app completa con rutas
SVELTEKIT: LA APP REAL (ROUTING + SSR + FORMULARIOS) ━━━━━━━━━━━━━━━━━━━━━━━━━━━   npm create svelte@latest miapp && cd miapp && npm install && npm run dev  ROUTING POR ARCHIVOS (c…

---
✅ 3 lecciones · 📝 6 preguntas de repaso en quizzes_html/ · tests/