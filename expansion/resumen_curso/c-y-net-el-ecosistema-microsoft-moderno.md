# 📕 Resumen maestro — C# y .NET — El Ecosistema Microsoft Moderno

> Todo el curso en una hoja: una idea núcleo por lección. Releer semanal.

## 1. 1. C#: lenguaje que ya no es 'de Windows'
C#/.NET: MULTIPLATAFORMA Y RÁPIDO ━━━━━━━━━━━━━━━━━━━━━━━━━━━ .NET moderno (6+) corre Linux/Mac/Windows, es de los backends más rápidos del mundo (benchmarks top), y C# es un TypeS…

## 2. 2. ASP.NET Minimal API: el Express de .NET
API REST EN 15 LÍNEAS CON .NET ━━━━━━━━━━━━━━━━━━━━━━━━━━━   dotnet new web -o Api && cd Api    // Program.cs COMPLETO:   var builder = WebApplication.CreateBuilder(args);   var ap…

## 3. 3. Entity Framework Core: la BD como objetos C#
EF CORE: EL ORM DE .NET ━━━━━━━━━━━━━━━━━━━━━━━━━━━   dotnet add package Microsoft.EntityFrameworkCore.Sqlite    // El modelo y la 'sesión' con la BD:   class Tarea { public int Id…

## 4. 4. Proyecto: API .NET completa con EF Core
CONSTRUYE: API DE TAREAS .NET SERIA ━━━━━━━━━━━━━━━━━━━━━━━━━━━ 1. dotnet new web -o Tareas.Api && cd Tareas.Api 2. dotnet add package Microsoft.EntityFrameworkCore.Sqlite 3. Crea …

---
✅ 4 lecciones · 📝 8 preguntas de repaso en quizzes_html/ · tests/